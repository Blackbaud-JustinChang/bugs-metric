$("#currentGraphsBorder").html("<% if !@graphs.empty? %>\
                                <%= escape_javascript(render @graphs) %>\
                                <% else %>\
                                No graphs have been added\
                                <% end %>");