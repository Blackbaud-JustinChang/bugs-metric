<li id="graph_<%= graph.id %>">
  <%= graph.id %> <%= graph.name %> <%= graph.search %> <%= graph.in_summary %> <%= link_to "Remove", {:controller => "graphs", :id => graph.id, :action => "remove_graph"} %>
</li>